# Optional: Entra ID (Azure AD) app registration for browser-based Fabric auth.
# When set, the Push to Fabric modal uses MSAL redirect auth instead of manual token entry.
# See README.md for setup instructions.
VITE_FABRIC_CLIENT_ID=
